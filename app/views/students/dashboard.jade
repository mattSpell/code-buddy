extends ../shared/template

block content
  h1 Student Dashboard
  h3= student.name

  if(student.completedCourses)
    each course in student.completedCourses
      .completecourses
        h3= course.title
        p= course.description
        p #{course.results.grade}%
